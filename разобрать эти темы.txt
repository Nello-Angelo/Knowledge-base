[kubernetes]
как логиниться под сервис аккаунтом
почему пробы отрабатывают только по 80 порту
Существует также более новый сторонний проект, JenkinsX (jenkins-x.io), предназначенный для запуска Jenkins в кластере Kubernetes
docker-registry secret
конфигмап 
логи в stdout и stderr
монтирование пода в папку 
LimitRange

apiVersion: v1
kind: ResourceQuota  (выяснить почему создается меньше чем указанно в параметрах, всего что указано по одному)
metadata:
  name: object-counts
spec:
  hard:
    count/deployments.apps: "4"
    count/pods: "4"
    count/replicasets.apps: "4"

Создание кластера etcd
Сохраните следующий код в файл etcd-cluster.yaml:
apiVersion: "etcd.database.coreos.com/v1beta2"
kind: "EtcdCluster"
metadata:
  name: "etcd-cluster"
spec:
  size: 3
  version: "3.2.13"

[ansible]